# Screenshot

`viewer.png` is a Chromium screenshot of Datumfold showing the block from the
current controlled RGB benchmark. The source images are project-generated
under Apache-2.0. The benchmark supplies calibrated cameras and masks.
The exported STEP is kernel-valid; this screenshot demonstrates the workspace.
