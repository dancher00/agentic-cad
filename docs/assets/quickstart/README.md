# Workspace screenshot

`viewer.png` shows Agentic CAD's actual offline viewer, generated from the
GPT-5.6 Sol four-view L-bracket integration check. The four reference views are
project-generated fixtures, not photographs of a physical object.

Captured in Chromium at 1440 × 1000 CSS pixels, 2× scale. The viewer displays
the exported model, measured CAD bounding-box extents and program dimensions.
This image illustrates usage; it is not an accuracy benchmark.
