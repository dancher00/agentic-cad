# Workflow figure

One figure presents the model/tool pipeline and its input, intermediate and output data.
The native SVG and editable Excalidraw describe the current GPT reconstruction path.
The code excerpt is schematic. The PNG is a browser rendering of the SVG.
FEM and grasp planning are downstream consumers, not GPT operations.
